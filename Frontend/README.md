# Notice: Active Frontend is in `/client`

> [!NOTE]
> The primary, production-ready React client is located in the `client/` directory.
> All builds, dependencies (`socket.io-client`, `axios`, etc.), and orchestration scripts (`start-all.bat`, `docker-compose.yml`) operate from `client/`.
> Screenshots have been preserved in `docs/Screenshots/`.
